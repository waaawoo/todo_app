import React from 'react'

class Form extends React.Component{
  render(){
    return (
      <form action="">
          <input type="text"/>
          <button>追加</button>
        </form>
    );
  }
}

export default Form;
